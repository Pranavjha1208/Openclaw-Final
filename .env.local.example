# Copy to .env.local and fill. Used by start-fixit.sh. Never commit .env.local.
# On another laptop: copy this file to .env.local and paste your values.

GEMINI_API_KEY=
FIXIT_MONGO_URI=
FIXIT_JWT_SECRET=
OPENCLAW_GATEWAY_AUTH_TOKEN=
